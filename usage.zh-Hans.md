# Anime SDF Gen — 简体中文使用指南

版本 **0.16.0**。维护者：[xht8723](https://github.com/xht8723)。适用于 Windows 上的 Blender **5.2 LTS**。

## 安装与语言

在 Blender 偏好设置中选择 **从磁盘安装（Install from Disk）**，安装 `anime_sdf_gen-0.16.0.zip` 并启用扩展。在原始 3D 视图按 **N**，打开 **Anime SDF Gen** 标签页。

同一个安装包包含英文和简体中文。在 **编辑 → 偏好设置 → 界面 → 语言** 中选择 **简体中文**，或使用 Blender 的 **自动** 语言设置。插件跟随 Blender 实际采用的语言。其他语言下，插件自有文本回退为英文。

Blender 的 **界面、工具提示、报告** 翻译选项分别控制对应文本。例如，关闭“报告”翻译后，插件底部的诊断正文保留英文。切换语言会刷新已打开的编辑窗口，并保留曲线、选择、撤销历史、播放状态和正在运行的生成任务，无需重启编辑会话。原生对话框遵循 Blender 自身的刷新行为。

语言只影响显示。对象名、UV 名、顶点组名、用户文件名、工程字段和纹理数值不会被改名或翻译。生成结果加载时仍应使用 **Non-Color** 数据色彩空间。

更新扩展版本前，请先关闭编辑会话；安装更新后重启 Blender，使新的 Python 模块完整加载。单纯切换语言不需要重启。

## 选择面部

- **选中的面**：激活网格，进入编辑模式，选择面部区域。点、边、面选择模式均支持，但仅包含完整选中且可见的多边形。多对象编辑模式只读取活动网格。
- **顶点组**：选择面部顶点组。一个多边形的所有顶点都必须在此组中具有正权重。对象模式和编辑模式均可使用。

需要已有的 UV：所选区域位于单个 **0–1** 区域内，面部三角形没有重叠或零面积。不要求对称 UV，也不要求按特定方向摆放。左右面部共享相同纹素时，无法储存两边不同的阈值；镜像绘制不能解决这一问题。

每次完整扫掠默认 **9 个阴影关键帧**，可设置为 **2–33**，包含首尾两帧。点击 **创建** 后，插件捕获面部选择和 UV。之后修改 Blender 中的选择不会悄悄改变编辑区域。

测试场景中可通过材质选择 `body` 上使用 `head` 材质的面。这只是测试模型的选择方式，插件不要求这些名称。

## 四步编辑

1. **将面部调整为正视图。** 旋转视图，让角色正对你且头部朝上。通过 **源模型 / UV…** 查看或更换 UV。默认启用 **仅正面表面**。点击 **下一步**。
2. **放置标记。** 鼻子、嘴部中心、下巴标记已放在近似位置，拖动到对应部位。鼻子应高于嘴部中心，嘴部中心应高于下巴。选择预设和关键帧数，然后继续。
3. **调整曲线。** 左侧显示面部参考和可编辑曲线，右侧实时显示阴影。初始选中“左 → 右”的第一个关键帧。逐帧调整后点击 **下一步**。
4. **确认。** 一个大预览窗口自动播放 **正面 → 角色左侧 → 背面 → 角色右侧 → 正面** 的完整光照旋转。设置输出后，点击 **生成并完成**。

**简洁面部** 预设的所有主边界都是直线；**鼻部强化** 预设提供更明显的鼻部轮廓。标记位置用于拟合，并非自动识别的解剖位置。

编辑器是独立的浮动 Blender 窗口，隐藏工作区标签、菜单和无关面板。右侧控件与底部警告区可滚动。上一步箭头位于大按钮旁边；第一步中不可用。将鼠标停在按钮上可查看说明；禁用的按钮会解释原因。

## 视图与曲线操作

视图操作使用 Blender 当前导航键位。默认 **鼠标中键** 旋转，**Shift + 中键** 平移，滚轮缩放；**小键盘 1 / 3 / 7** 对应前、右、顶视图。曲线编辑视图固定在正交投影平面，可以平移和缩放；在预览视图中旋转查看效果。**居中面部** 可重新框住面部。

**聚焦** 或视图内的 **Ctrl+Space** 放大单个视图，**并排视图** 恢复左右布局。可拖动中间分隔线改变宽度。

- 点击控制点选择；**Shift + 点击** 增减选择。拖动所选点可一起移动；拖动控制柄只调整该控制柄。
- **B** 框选；框选手势中按 Shift 或使用中键减选。直接从空白处拖动也可框选，此时 Shift 加选、Ctrl 减选。
- **A** 全选，**Alt+A** 取消全选，**Ctrl+I** 反选。
- **G / R / S** 移动、旋转、缩放；**X / Y** 约束轴向。可直接输入数值，Enter 确认，Esc 取消。例如 **G X .05 Enter** 沿面部宽度移动 5%；**R 15 Enter** 旋转 15°。
- **Shift** 精细调整；**Ctrl** 增量吸附；两者组合可细化吸附步长。
- **平滑、尖角、自由** 控制柄决定曲线形状。**插入** 细分下一段，**移除** 删除控制点。主边界端点及闭合曲线所需的最少点数会保留。
- 点击图层选中整条曲线；**移动 / 旋转 / 缩放…** 和 **控制点** 对话框支持精确数值。
- **Ctrl+Z / Ctrl+Shift+Z** 撤销和重做本次编辑。一次拖动对应一次撤销。

## 两次边界扫掠与亮部

两行表示正面投影中阴影边界的移动方向：

- **左 → 右**：阴影从视图左侧扩展到整张面部。
- **右 → 左**：阴影从视图右侧扩展到整张面部。

每一行都是完整扫掠，拥有自己的首尾关键帧。默认首帧全亮、末帧全暗，没有共用的中间帧。**镜像完整扫掠** 将整行反射到另一行，保持关键帧顺序；关闭后可独立设计两行。

**添加三角形** 创建一条平滑的三点闭合曲线。选择 **亮部** 可在阴影内部挖出亮区，选择 **暗部** 可添加阴影。亮部会应用到同次扫掠的更早关键帧，保持阈值纹理所需的单向过渡。图层顺序决定重叠形状的运算顺序。

若像素在亮部继承范围之外重新变亮，底部会指出有冲突的扫掠和关键帧，预览高亮冲突区域。修正后才能生成。360° 首尾接缝警告提醒两行端点不连续，但不阻止导出。

## 输出

**确认** 步骤设置保存位置、基础文件名、分辨率、输出精度、平滑强度和通道打包。

| 精度 | 文件 |
|---|---|
| 8 位 | PNG，文件较小、量化较粗 |
| **16 位（默认）** | PNG |
| 32 位浮点 | EXR，完整 float32 精度 |

每张纹理均可选择 **R / G / B** 通道，或 **单独保存** 为灰度图。默认 R 为左侧阴影阈值，G 为右侧阴影阈值，B 为面部覆盖范围。切换精度会更新扩展名和待生成文件列表。

**平滑强度** 范围为 **0–8**，默认 **0 / 关闭**。它在 UV 转移前平滑阈值，可能改变细小形状和切换时机；不会让硬截断着色器产生柔边阴影。覆盖范围和恒亮、恒暗端点不参与平滑。拖动曲线仍显示直接编辑的遮罩，插值播放显示平滑后的效果。

纹理数值 **0** 表示恒暗，**1** 表示恒亮，其余值表示阴影切换阈值。着色器必须处理这两个端点，按光照方向选择正确纹理，并遵循覆盖范围。完整约定见 [输出格式](output-format.md)。

**生成并完成** 验证后一起写入纹理和 `.sdfproject.json` 工程，随后关闭临时编辑窗口并恢复原工作区。失败或取消生成会保留编辑会话；已存在的输出文件保持原样。插件不会自动给原模型分配材质。

## 保存与恢复

- **保存草稿并关闭**：保存可编辑工程，关闭窗口。
- **放弃并关闭** 或窗口 **X**：结束当前编辑，保留明确保存过的文件。
- **编辑已有 SDF**：重新打开 schema 4 工程。若源模型已改变，使用 **重新关联活动源模型**，然后重新定向和拟合。
- **恢复最新草稿**：打开 Blender 用户配置目录下最新的自动恢复草稿。

保存 `.blend` 时，插件会先移除临时预览，保存完成或失败后再重建。原始模型、UV、材质及工作区受到保护。加载其他文件或禁用插件时会保留恢复草稿。旧工程格式会明确拒绝，不进行迁移。

当前版本使用单个正面投影，无法独立处理后脑、被遮挡的凹陷或下方区域。**面部覆盖范围** 标记支持的表面，最终着色器应限制在预期面部区域。

[English guide](usage.md) · [验证结果](validation.md)
